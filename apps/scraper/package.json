{
  "name": "scraper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "description": "Playwright worker that drives SCDB and captures CSV downloads. Invoked by Laravel jobs on the VPS \u2014 never by the Vercel frontend.",
  "bin": {
    "iqradb-scraper": "bin/scraper.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "lint": "node --check bin/scraper.mjs && node scripts/check-syntax.mjs",
    "typecheck": "echo \"No TypeScript in apps/scraper \u2014 recipe shapes are validated at runtime by src/recipe.mjs\"",
    "install-browser": "playwright-core install --with-deps chromium",
    "install-browser:no-deps": "playwright-core install chromium"
  },
  "dependencies": {
    "playwright-core": "1.56.1"
  }
}
